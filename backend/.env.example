# ==============================================
# SECURITY WARNING - 安全警告
# ==============================================
# This is an EXAMPLE file only. DO NOT commit actual .env files!
# 这只是示例文件。切勿提交实际的.env文件！
#
# How to generate secure secrets - 如何生成安全密钥:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# After creating your .env file:
# 1. Copy this file to .env
# 2. Generate secure secrets using the command above
# 3. Update DATABASE_URL with your actual credentials
# 4. Never commit .env to version control
# ==============================================

# Application
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Database
# Replace with your actual PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL=postgresql://postgres:password@localhost:5432/cac_ecommerce?schema=public

# JWT Authentication
# CRITICAL: Generate a strong random secret using:
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# NEVER use the example value below in production!
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_64_BYTE_HEX_STRING
JWT_EXPIRES_IN=7d

# Bcrypt
BCRYPT_ROUNDS=10

# Optional: File Upload
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./uploads

# Optional: Email Service (if you add email functionality)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASS=your-email-password
# SMTP_FROM=noreply@example.com

# Optional: Payment Gateway (if you add payment functionality)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Optional: AWS S3 (if you use cloud storage)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=your-bucket-name
